#!/usr/bin/env bash
set -euo pipefail

name=${1:?usage: dev-register <name> <port> [ttl]}
port=${2:?usage: dev-register <name> <port> [ttl]}
ttl=${3:-4h}

# TODO: implement tunnel/proxy registration
# Should expose localhost:<port> at <name>.demo.nikmel.dev with the given TTL
